<h3><%= @user.username %></h3>
<br>
Posts:
  <ul>
  <% @posts.each do |post| %>
    <li>
    <%= link_to post.title, "#{post.url}" %>
    <%= link_to "#{pluralize(post.comments.count, "comment")}", post_path(post) %>
    <%= link_to "edit", edit_post_path(post) %>
    </li>
  <% end %>
  </ul>

Comments:
  <ul>
  <% @comments.each do |comment| %>
    <li>
    <%= comment.body %><br />
    (made on post <%= link_to comment.post.title, post_path(comment.post) %>)
    </li>
  <% end %>
  </ul>